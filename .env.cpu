# CPU-only inference (~1-3 min per generation)
# Models are cast to float32 to avoid the bfloat16 performance bug on AMD CPUs
DEVICE=cpu
MODEL_VARIANT=lite
MODEL_CACHE_DIR=/data/models
ENABLE_PROMPT_REWRITE=false
DEFAULT_NUM_SEEDS=1
PORT=8100
